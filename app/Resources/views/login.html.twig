<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>BadZen - Login Page</title>
        <link rel="stylesheet" href="{{ asset('css/bootstrap.min.css') }}" />
        <link rel="stylesheet" href="{{ asset('css/signin.css') }}" />
        {% block stylesheets %}{% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('icon-192x192.png') }}" />
    </head>

    <body class="text-center">
      <form class="form-signin" action="{{ path('login') }}" method="post">
        <img class="mb-4" src="{{ asset('icon-192x192.png') }}" alt="" width="72" height="72">
        <h1 class="h3 mb-3 font-weight-normal">Authentification</h1>
        {% if error %}
            <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
        {% endif %}
        <label for="username" class="sr-only">Utilisateur</label>
        <input type="text" id="username" name="_username" value="{{ last_username }}" class="form-control" placeholder="Utilisateur" required="" autofocus="">
        <label for="password" class="sr-only">Mot de passe</label>
        <input type="password" id="password" name="_password" class="form-control" placeholder="Mot de passe" required="">
        <button class="btn btn-lg btn-primary btn-block" type="submit">S'Authentifier</button>
        <p class="mt-5 mb-3 text-muted">© 2017-2018</p>
      </form>

    <script src="{{ asset('js/bootstrap.min.js') }}"></script>
    </body>
</html>
