{% extends 'base.html.twig' %}

{% block body %}
    <h1>Type</h1>

    <table class="table table-striped table-sm table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ type.nom }}</td>
            </tr>
            <tr>
                <th>Nombre_poules</th>
                <td>{{ type.nombrepoules }}</td>
            </tr>
            <tr>
                <th>Taille_poule</th>
                <td>{{ type.taillepoule }}</td>
            </tr>
            <tr><th></th><td></td></tr>
            <tr><th></th><td></td></tr>
            <tr><th></th><td></td></tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('type_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('type_edit', { 'id': type.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
